@model IEnumerable<Tienda.Core.Prestamo>
@{
    
    ViewBag.Title = "Generar";
}

<h2>Generar</h2>

@*@using (Html.BeginForm())
{
    <tbody>
        @foreach (var item in Model)
        {
            <div class="form-group">
                @Html.LabelFor(model => model.Fecha)
                @Html.EditorFor(model => model.Fecha, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.Cliente)
                @Html.EditorFor(model => model.Cliente, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.Monto)
                @Html.EditorFor(model => model.Monto, new { @class = "form-control" })
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.Cuotas)
                @Html.EditorFor(model => model.Cuotas, new { @class = "form-control" })
            </div>
            <div class="form-group">
                <input type="submit" value="Registrar" class="btn btn-primary" />
            </div>
            </tbody>
        }*@
        